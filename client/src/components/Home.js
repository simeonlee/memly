import React from 'react'

const Home = (props) => {
  return(
    <div id ="Home">
      <h1> Memly </h1>
    </div>
    )
}

export default Home

//props.children will render any sub routes specified by react-router!