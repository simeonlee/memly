import React from 'react'

const Home = (props) => {
  return(
    <div>
      <h1> Home Page </h1>
    </div>
    )
}

export default Home

//props.children will render any sub routes specified by react-router!